#!/usr/bin/env bash
cat <<'EOF'

Chezmoi bootstrap finished.

From a TTY you can start the desktop with:
  Hyprland

Recommended after first apply:
  1. Reboot, so group membership, fish shell, portals, Bluetooth, and NetworkManager settle.
  2. Log in and run Hyprland.
  3. Press SUPER+Enter for terminal and SUPER+T for wallpaper.

Private SSH/password aliases were intentionally not imported.
Put private aliases in:
  ~/.config/fish/conf.d/private-aliases.fish

EOF
